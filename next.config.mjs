/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["localhost"], // Añade aquí cualquier otro dominio necesario.
  },
};

export default nextConfig;
